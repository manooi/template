# AGENTS.md

Agent instructions for this repo live in [CLAUDE.md](./CLAUDE.md) — read that
file; everything there applies to any coding agent, not just Claude.

Maintenance: either replace this stub with a symlink (`ln -sf CLAUDE.md
AGENTS.md`) or mirror substantive CLAUDE.md edits here manually. Do not let
the two drift — a rule that exists in only one file is a rule half the agents
don't know.
