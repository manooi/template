# Key files — per-file index

Full map of the source tree: one line per file — what it owns, what depends on
it. CLAUDE.md keeps a 5-bullet orientation; this file holds the rest.

Update when files are added/moved (cheap to maintain if each landing commit
touches the row it changes).

## Layout

```
{{src tree sketch}}
```

## Index

| File | Owns | Notes |
|---|---|---|
| `{{src/...}}` | {{responsibility}} | {{gotcha / who calls it}} |
