# Roadmap

Numbered feature backlog. Rules:

- Every feature gets a stable number `#N` — never reused, never renumbered.
- When implementing an item, tick it `[x]` **in the same commit** as the change.
- The planning tab adds items (on `main`); the dev orchestrator ticks
  checkboxes (on bead branches). This is the one file both sessions write —
  rebase conflicts here are trivial, keep both edits.
- Epics get a companion `plans/epic-*.md`; link it from the item.

## What's Well Implemented

(Keep a short list of what already works — it's the "don't rebuild this" guard.)

## Quick Wins

- [ ] `#1` **High** {{first small item}}

## Missing Features

### {{Area A}}

- [ ] `#2` {{item}}
- [ ] `#3` {{item}}

### {{Area B}}

- [ ] `#4` {{item}}

## Ideas / Someday

- [ ] `#5` {{stretch item — cheap to write down, free to ignore}}
